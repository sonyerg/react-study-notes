const hobbies = ["Gaming", "Sports", "Music"];
// creates a constant hobby for every item in hobbies
for (const hobby of hobbies) {
  console.log(hobby);
  // this will execute thrice because there are 3 items in hobbies array
}

const a = prompt("Hello");

if (a === "ello") {
  // if first condition is true execute here
} else if (a === "hello") {
  // if the first condition is not met, excute second condition code here.
  // you can add more else if, but only one else.
} else {
  // if no condition is met, excute code here
}
